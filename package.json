{
  "name": "cloudflare-vault",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker for the Task #227 single-user cloud vault test.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "schema:remote": "wrangler d1 execute vault-test-meta --remote --file=./schema.sql",
    "schema:local": "wrangler d1 execute vault-test-meta --local --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250501.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.80.0"
  }
}
